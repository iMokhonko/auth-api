module.exports = async (event, context, next, rateLimitHeaders) => {
  const nextResult = await next(event, context);

  return {
    ...nextResult,
    headers: {
      ...nextResult.headers,
      ...rateLimitHeaders
    }
  }
}